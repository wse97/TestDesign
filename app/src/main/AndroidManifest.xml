<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrwaseem.testdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestDesign">
        <activity
            android:name=".ui.activity.loginActivity.LoginActivity"
            android:label="@string/title_activity_details"
            android:theme="@style/Theme.TestDesign.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.detailsActivity.DetailsActivity" />
        <activity android:name=".ui.activity.mainActivity.MainActivity">

        </activity>
    </application>

</manifest>